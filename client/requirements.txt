# GUI Framework
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-Charts==6.6.0
PyQt6-Charts-Qt6==6.6.0
pyqtgraph==0.13.3

# Networking
requests==2.33.0  # Security: Fixes CVE-2024-47081 (netrc leak), CVE-2024-35195 (verify=False persistence), PYSEC-2026-2275
websockets==12.0
aiohttp==3.14.3  # Security: Fixes CVE-2024-23334 (directory traversal), CVE-2024-30251 (DoS), CVE-2024-52304 (request smuggling), CVE-2024-27306 (XSS), CVE-2024-23829 (HTTP parser), CVE-2025-53643 (smuggling), PYSEC-2026-2094..2107, -3545, -3546, -3547

# SSH and Deployment
paramiko==3.4.0  # NOTE: PYSEC-2026-2858 (rsakey.py permits SHA-1) is only fixed in 5.0.0,
                 # a major bump to the SSH host-key path used by the deploy wizard.
                 # Deferred: that path has no test coverage and needs a real SSH target.
scp==0.14.5

# Network Discovery
# scapy removed due to security vulnerability with no patch (pickle deserialization RCE, <=2.6.1)
# Not used in codebase - was planned for network scanning but never implemented
zeroconf==0.149.16  # Security: Fixes PYSEC-2026-3435, -3436, -3437, -3438, -3439

# Data Handling
numpy==1.26.3
pandas==2.2.0
h5py==3.10.0

# Configuration
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.2  # Security: Fixes PYSEC-2026-2270

# Utilities
python-dateutil==2.8.2
qasync
keyring==25.4.1
