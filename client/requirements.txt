# LabLink Client dependencies (2.x baseline)
#
# Pinned exactly for reproducible builds. Security floors use >= so a patched
# release can be picked up without editing this file.

# GUI Framework
PyQt6==6.11.0
PyQt6-Qt6==6.11.0
PyQt6-Charts==6.11.0
PyQt6-Charts-Qt6==6.11.0
pyqtgraph==0.14.0
qasync==0.28.0

# Networking
requests==2.34.2
websockets==17.1
aiohttp==3.14.3

# Remote deployment
# paramiko 5.x drops ssh-rsa (RSA/SHA-1) host keys and DSA entirely; see
# BREAKING-CHANGES in the 2.0.0 release notes.
paramiko==5.0.0
scp==0.16.1

# Discovery
zeroconf==0.151.3

# Data Handling
numpy==2.5.2
pandas==3.0.5
h5py==3.16.0

# Models / configuration
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.2
python-dateutil==2.9.0.post0

# Credential storage
keyring==25.7.0
